# Workspace identity and project context

This workspace is the **Share Knowledge Free** project. Its product is named **Branch**. The Git remote points to `LuisTGeo/share-knowledge-free`.

Use the root `README.md` and the current Branch implementation roadmap as the authoritative project context. The product and app are named Branch.

Current workflow constraints from the user:

- Keep AI/model implementation, project setup, stack selection, commands, dependencies, arbitrary repository execution, and changes to forked projects for the final paired step.
- Do not make project code or infrastructure changes during catalog/search architecture planning.
- The catalog data model and curation lifecycle design is complete in Notion. Search-engine selection is waiting on the paired datastore/setup decision. The next roadmap phase is Phase 9, beginning with observation of a current workspace/data-boundary journey.
